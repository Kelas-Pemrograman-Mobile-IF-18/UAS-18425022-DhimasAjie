<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.david.projekdhimas">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.c2dm.permission.RECEIVE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/Theme.Projekdhimas">
        <activity android:name=".pelanggan.DetailHotel"></activity>
        <activity android:name=".admin.DataHotel" />
        <activity android:name=".admin.EditData" />
        <activity android:name=".admin.InputData" />
        <activity android:name=".admin.Profile" />
        <activity android:name=".pelanggan.HomeUser" />
        <activity android:name=".user.Register" />
        <activity android:name=".admin.HomeAdmin" />
        <activity android:name=".user.Login">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>